hub:
  config:
    Authenticator:
      admin_users:
        - admin
      allowed_users:
        - user
    DummyAuthenticator:
      password: example
    JupyterHub:
      authenticator_class: dummy
